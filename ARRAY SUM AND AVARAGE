#include<stdio.h>

int main()
{
    int a[5], sum = 0, i;
    float avg;

    printf("Enter 5 values:\n");
    for(i = 0; i < 5; i++)
    {
        scanf("%d", &a[i]);
    }

    for(i = 0; i < 5; i++)
    {
        sum += a[i];
    }

    avg = sum / 5.0;  // use 5.0 to ensure float division

    printf("The sum is %d\n"
