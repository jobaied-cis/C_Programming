#include<stdio.h>
int main()
{
    int a[100],sum=0,i;
    printf("Enter the value :");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    int max=num[0];
    for(i=0;i<n;i++)
    {
      if(max<num[1])
            max=max[i];
    }
    printf("maximum %d\n",max);

}
